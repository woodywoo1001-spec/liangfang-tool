# GitHub Pages 一键部署

在 Mac 终端执行以下命令：

## 如果已安装 gh CLI

```bash
# 启用 GitHub Pages（只需执行一次）
gh api /repos/woodywoo1001-spec/liangfang-tool/pages \
  -X POST \
  -F "source[branch]=main" \
  -F "source[path]=/"
```

## 如果没装 gh CLI，手动操作

1. 打开 https://github.com/woodywoo1001-spec/liangfang-tool/settings/pages
2. Branch 选择 `main`，目录选 `/ (root)`
3. 点击 Save
4. 等 30 秒后访问 https://woodywoo1001-spec.github.io/liangfang-tool/
